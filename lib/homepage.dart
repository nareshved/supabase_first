import 'package:flutter/material.dart';

class SupabaseHome extends StatelessWidget {
  const SupabaseHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("dfdf"),
      ),
    );
  }
}
